# Admin login for /admin.html (Basic Auth for admin APIs)
ADMIN_USER=admin
ADMIN_PASSWORD=admin123
# Server
PORT=3000
# MySQL / MariaDB (XAMPP)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
# If you can run: mysql -u root  (no -p) successfully, the password is EMPTY.
MYSQL_PASSWORD=root
MYSQL_DATABASE=scsp1
